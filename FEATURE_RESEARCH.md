# Панель из открытых источников и отбор характеристик

Сентябрь 2026. Работа над двумя проблемами: в репозитории не было данных для
обучения (один срез вместо панели) и не было способа проверить, помогает ли
добавленная характеристика.

Итог коротко: панель собрана, CA0/CA1/MLP на ней обучаются, **ни одна из 13
проверенных новых характеристик не улучшает результат значимо**, и это измерено, а
не предположено. Попутно нашлись три вещи, которые важнее одной фичи: headline-Sharpe
статьи не переносится на large caps, ранг важности характеристик не переносится между
панелями, а дефолтная ранняя остановка превращала сравнение моделей в шум.

---

## 1. Данные

`clean_data.csv` содержит 317 акций на одну дату (31.12.2015). Модель вида
r_t = β(z_{t-1})ᵀ f_t требует панели: характеристики на конец месяца t и доходность
за t+1. На одном срезе обучающий пример ровно один, а временное разбиение
невозможно в принципе.

`build_panel.py` собирает месячную панель бесплатно, без WRDS:

| источник | что даёт |
|---|---|
| [fja05680/sp500](https://github.com/fja05680/sp500) | состав S&P 500 на каждую дату (point-in-time) |
| Yahoo Finance (`yfinance`) | дневные цены, объёмы, дивиденды, сплиты |
| SEC EDGAR XBRL companyfacts | отчётность **вместе с датой подачи** |
| Yahoo `^IRX` | безрисковая ставка (статья моделирует сверхдоходности) |

### Что получилось

| | |
|---|---|
| Наблюдений | 77 588 (было 317) |
| Период | 2010-01 … 2025-11, 191 месяц |
| Тикеров | 588, по 320–494 в каждом месяце |
| Характеристик | 31, ранг-нормированы в [−1, 1] внутри месяца (как у GKX) |
| Средняя сверхдоходность | 1.01% в месяц |
| Тестовое окно | 38 месяцев (2022-10 … 2025-11) |

### Контроль утечек

Показатель попадает в панель только с месяца, когда он **опубликован**: EDGAR
отдаёт дату подачи, и `asof_to_months` берёт последнее значение, поданное до конца
месяца. Годовая отчётность за 2015 год выходит в феврале–марте 2016-го, поэтому на
31.12.2015 её в признаках быть не может.

Проверки, которые прошли:

* **Структурный тест.** `ret_next[t]` совпадает с фактической доходностью месяца
  t+1 в 99.98% пар (остаток — разрывы в истории тикеров).
* **Корреляции с таргетом.** Все |r| < 0.02. При утечке они были бы 0.3+.
* **Знаки совпадают с литературой.** `mom1m` отрицательный (краткосрочный разворот),
  `bm` отрицательный — в 2010–2025 growth действительно бил value.

### Две ошибки данных, которые пришлось починить

**Неучтённые сплиты.** У `CPWR` цена скакала ровно в 10 раз вниз и в 4 раза вверх:
Yahoo не записал сплиты у делистнутого тикера, и в таргете это выглядело как ±90% за
месяц. `drop_corrupted` ловит скачок ровно в целое число раз при отсутствии
записанного сплита — поймал 4 тикера (`CPWR`, `GR`, `EP`, `MI`), все делистнутые, ни
одной живой компании не задел.

**Таймауты как делистинг.** `yfinance` на таймаут отвечает «possibly delisted», и
живые компании (`ICE`, `FITB`, `HII`) молча выпадали из выборки. Добавлены повторные
проходы мелкими партиями; кэш пишется по частям, поэтому обрыв не обнуляет прогресс.

### Ограничения, которые нужно называть

* **Смещение выживших.** Yahoo и SEC не знают многих обанкротившихся и поглощённых
  тикеров, выборка перекошена в пользу выживших.
* **Фундаментал с ~2009 года** — раньше в EDGAR нет XBRL.
* **Только large caps.** S&P 500 вырезает мелкие и неликвидные акции. Это оказалось
  главным ограничением, см. раздел 5.

---

## 2. Модели на реальных данных

Пакет `asset_pricing` (эта же ветка) уже реализует CA0/CA1/MLP корректно: лаг на
месяц, managed-портфели по уравнению (16), временное разбиение 60/20/20, прогноз
только по прошлым премиям факторов. Переписывать его не потребовалось — не хватало
только данных.

Контракт CSV: `prepare_panel` лагирует признаки **сам**, поэтому в
`data/panel_model.csv` колонка `returns` — сверхдоходность **того же** месяца, а
характеристики отдаются сырыми. Если подать туда доходность следующего месяца, лаг
применится дважды.

Ансамбль из 8 сидов (усреднение прогнозов — приём GKX), K=5, 800 эпох, patience 200:

| модель | predictive R² | Sharpe EW | Sharpe VW |
|---|---:|---:|---:|
| CA0 | 0.995% | 0.356 | 1.011 |
| CA1 | 0.771% | 0.140 | 0.626 |
| MLP | 0.874% | 0.061 | 0.275 |

CA обходит прямой MLP, то есть факторная структура помогает — но выигрыш скромный.
CA1 ≈ CA0, то есть нелинейность в beta-сети не даёт ничего: на large caps связь
достаточно близка к линейной. В статье нелинейность помогала (CA1 0.53% против IPCA
0.3%), у нас — нет.

### Дефолтная ранняя остановка ломала сравнение

С дефолтами `patience=40, epochs=300` рейтинг моделей **переворачивался** от сида к
сиду. Причина: для месячных доходностей прогноз «ноль» очень хорош, поэтому почти
необученная сеть показывает низкий validation MSE, и 40 эпох терпения не дают из этой
ямы выбраться. Развалилось 4 прогона из 12 (остановка на эпохе 1–12), и именно они
определяли «победителя».

| сид | CA0 | CA1 | MLP |
|---|---:|---:|---:|
| 42 | **−0.50** (эпоха 2) | 0.71 | 0.10 |
| 1 | 0.81 | **−0.23** (эпоха 1) | 0.35 |
| 7 | 0.98 | **−0.23** (эпоха 12) | 0.62 |
| 2026 | 0.92 | **−1.65** (эпоха 1) | 0.30 |

С `patience 200, epochs 800` развалы исчезают, а прогоны, где ранняя остановка не
срабатывала, дают **в точности те же числа**. То есть это чистое исправление, а не
подгонка. MLP при `epochs=300` всегда упирался в максимум, то есть был недоучен.

**Следствие для `RESULTS.md`:** synthetic-вывод «прямой MLP лидирует на нелинейных
данных» получен на тех же дефолтах, и там же отмечено, что MLP выбрал максимальную
эпоху. Скорее всего, это артефакт ранней остановки, а не свойство моделей.

---

## 3. Отбор характеристик

Одиночный прогон меряет не качество признаков, а удачу инициализации: разброс
predictive R² между сидами ±0.12 п.п., вклад одной характеристики из 31 — около
0.03 п.п. Поэтому фичи отбирались двумя устойчивыми способами на всех 191 месяцах:

1. **Маржинальный IC** (`screen_features.py`) — то же, что variable importance в
   разделе 3.6 статьи, но без нейросети: регрессируем `ret_next` на уже имеющиеся
   признаки, берём остатки, считаем кросс-секционную корреляцию кандидата с ними.
2. **Decile long-short Sharpe** — метрика раздела 3.4: сортировка в децили по самой
   характеристике, лонг 10-го дециля, шорт 1-го, ребалансировка ежемесячно.

### Результаты по 13 кандидатам

| характеристика | канал | ранг в статье | IC t-stat | Sharpe EW | итог |
|---|---|---|---:|---:|---|
| `ear` реакция на отчёт (3 дня) | PEAD | ~75 | **+2.92** | 0.38 | лучшая из новых |
| `acc` начисления (Sloan) | качество прибыли | есть | +1.65 | −0.26 | слабо |
| `dy` дивидендная доходность | выплаты | ~18 | +1.54 | — | слабо |
| `roaq` рентабельность активов | качество прибыли | есть | +1.15 | 0.04 | шум |
| `egr` рост капитала | эмиссия | есть | −0.96 | −0.24 | шум |
| `chcsho` чистая эмиссия акций | эмиссия | есть | −0.81 | −0.16 | шум |
| `staleness` свежесть отчётности | **нет в статье** | — | −0.58 | — | шум |
| `cashdebt` покрытие долга | качество прибыли | есть | +0.34 | 0.10 | шум |
| `std_dolvol` разброс оборота | ликвидность | ~21 | −0.34 | — | шум |
| `aeavol` аномальный объём у отчёта | внимание | есть | −0.30 | 0.07 | шум |
| `indmom` отраслевой моментум | реляционный | **16** | −0.15 | 0.11 | шум |
| `idiovol` идиосинкр. волатильность | риск | **3** | −2.14 | 0.14 | R² не двигает |
| `baspread` спред | ликвидность | 10 | −3.12 | 0.12 | R² не двигает |

`indmom` считался как средний `mom12m` соседей по двузначному SIC **с исключением
самой акции** (иначе он частично дублирует её собственный моментум). SIC получен из
SEC submissions API для 587 из 588 тикеров, 58 отраслей.

### Почему избыточность важнее «важности»

Всё, что коррелирует с уже имеющимся `retvol` выше 0.85, бесполезно:
`baspread` 0.91, `idiovol` 0.89. Канал волатильности в панели насыщен. `ear`, напротив,
почти ортогонален всему (−0.07 с `retvol`, −0.05 с `idiovol`) — поэтому он и
единственный, у кого остался маржинальный сигнал.

Канал качества прибыли и эмиссии на large caps не работает целиком: пять
характеристик подряд дали шум.

### Проверка лучшего кандидата на модели

`ear` прогнан через ансамбль из 8 сидов, доверительные интервалы — бутстрап по
месяцам (`ensemble_ab.py`):

| модель | метрика | с `ear` | без `ear` | разница | 95% ДИ | P(>0) |
|---|---|---:|---:|---:|---|---:|
| CA0 | predictive R² | 0.937% | 0.995% | −0.058% | [−0.159, +0.053] | 0.14 |
| CA0 | Sharpe EW | 0.220 | 0.356 | −0.140 | [−0.313, +0.020] | 0.04 |
| CA0 | Sharpe VW | 0.879 | 1.011 | −0.142 | [−0.687, +0.371] | 0.32 |
| CA1 | predictive R² | 0.894% | 0.771% | +0.106% | [−0.539, +0.693] | 0.64 |
| CA1 | Sharpe EW | 0.189 | 0.140 | +0.056 | [−0.295, +0.439] | 0.60 |
| CA1 | Sharpe VW | 0.707 | 0.626 | +0.084 | [−0.722, +0.964] | 0.57 |
| MLP | predictive R² | 0.926% | 0.874% | +0.053% | [−0.048, +0.151] | 0.85 |
| MLP | Sharpe EW | 0.281 | 0.061 | +0.236 | [−0.285, +0.756] | 0.82 |
| MLP | Sharpe VW | 0.606 | 0.275 | +0.353 | [−0.149, +0.934] | 0.91 |

Ни одного значимого улучшения. У CA0 знак отрицательный.

### Утечка, которую пришлось убрать из `ear`

В первой версии трёхдневное окно после даты подачи заезжало в следующий месяц, если
отчёт вышел в последние дни месяца. IC был 0.017 при t = 4.58. После требования
«окно закрылось до конца месяца» стало 0.010 при t = 2.76. Разница целиком
объясняется утечкой — считать PEAD-признаки без этого условия нельзя.

---

## 4. Почему улучшение не измеримо — с числами

Тестовое окно — **38 месяцев**. При таком числе месяцев ширина доверительного
интервала на R² составляет ±0.1 п.п., а вклад одной характеристики из 31 — порядка
0.03 п.п. Эффект в три раза меньше разрешающей способности.

Сидами это не лечится: ансамбль из 8 сидов убрал шум инициализации, но остался шум
короткого теста. Чтобы сузить интервал до 0.03 п.п., нужно примерно **в 10 раз
больше тестовых месяцев** — около 360, ровно те 30 лет out-of-sample, которые есть у
статьи. У нас их не будет: XBRL в EDGAR начинается с 2009 года.

Поэтому отбирать характеристики по метрике модели на таком окне бессмысленно, и
маржинальный IC — не «замена попроще», а единственный способ с достаточной мощностью.

---

## 5. Главное расхождение со статьёй

Сравнение с таблицей 3 статьи при K=5:

| | наши | статья |
|---|---:|---:|
| CA0 Sharpe EW / VW | 0.36 / **1.01** | **2.10** / 0.83 |
| CA1 Sharpe EW / VW | 0.14 / **0.63** | **2.67** / 1.48 |

**Соотношение перевёрнуто.** У статьи equal-weight лучше value-weight в 2.5 раза, у
нас наоборот — VW лучше EW в 3–7 раз.

Скрининг объясняет почему: единственные характеристики с реальной силой в нашей
вселенной — `turn` (Sharpe EW 0.80, t = 3.21) и `dolvol` (0.60, t = 2.41), то есть
ликвидность. В статье EW-премия держится на мелких и неликвидных акциях, а в
S&P 500 их нет по определению. Их headline-результат 2.63 не переносится на large
caps — и не потому, что модель хуже, а потому что источник премии вырезан вместе с
вселенной.

Отдельно: по R² мы выглядим лучше статьи (0.9% против 0.53%), а по Sharpe хуже в
разы. Это ровно то, о чём авторы предупреждают в начале раздела 3.4 («трудно судить
об экономическом вкладе по R²»), воспроизведённое на своих данных.

**Вывод для дальнейшей работы:** узкое место — не список характеристик, а вселенная.
Если нужен рост метрик, расширять надо выборку за пределы S&P 500 (Russell 3000 или
все листингованные с фильтром по размеру), где `turn`, `ill` и `mvel1` работают в
разы сильнее. Пайплайн это переживёт: меняется только источник состава вселенной.

---

## 6. Воспроизведение

```bash
pip install yfinance pandas pyarrow requests torch
python build_panel.py --start 2010-01-01 --end 2025-12-31 --email you@hse.ru
```

Первый прогон — около часа (лимит запросов SEC), дальше всё из `data/cache`.
На выходе `data/panel.parquet` и `data/panel_model.csv`.

```bash
# модели на реальной панели
python -m asset_pricing compare --data data/panel_model.csv \
  --output artifacts/real --factors 5 --epochs 800 --patience 200

# отбор характеристик по маржинальному IC
python screen_features.py --email you@hse.ru

# сравнение двух наборов признаков по метрикам статьи
python ensemble_ab.py "artifacts/a_*" "artifacts/b_*" --labels с_фичей без_фичи
```

`--patience 200 --epochs 800` обязательны: с дефолтами часть прогонов не обучается,
см. раздел 2.

`data/` и `artifacts/` в `.gitignore` — панель занимает 2.4 ГБ вместе с кэшем.
